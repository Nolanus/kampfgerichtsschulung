# Docker Compose Environment Configuration
# ==========================================

# Version tag of the Docker image published to GitHub Container Registry (GHCR)
# Examples: latest, 1.0.0, 1.0, 1
TAG=latest

# Host port to expose the Kampfgericht application
PORT=3000
